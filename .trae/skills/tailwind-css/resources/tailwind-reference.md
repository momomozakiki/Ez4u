# Tailwind Reference

- Use project tokens from globals.css @theme (Tailwind v4).
- Prefer clsx for class composition; dedupe with tailwind-merge.
- Keep conditional logic out of JSX; extract before return.
- Extend via config only when primitives are insufficient.
